import * as phpStrings from 'locutus/php/strings/';
import * as phpXml from 'locutus/php/xml';

export const htmlEntityDecode = phpStrings.html_entity_decode;
export const htmlSpecialChars = phpStrings.htmlspecialchars;
export const utf8Encode = phpXml.utf8_encode;
